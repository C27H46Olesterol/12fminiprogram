<!--pages/client/product-news/product-news.wxml-->
<view class="container product-news-page">
  <!-- 顶部自定义 Tabs -->
  <view class="custom-tabs flex flex-center mb-20">
    <view class="tab-bg">
      <view 
        class="tab-item {{activeTab === 'news' ? 'active' : ''}}" 
        bindtap="onTabChange" 
        data-tab="news"
      >
        企业动态
      </view>
      <view 
        class="tab-item {{activeTab === 'product' ? 'active' : ''}}" 
        bindtap="onTabChange" 
        data-tab="product"
      >
        产品中心
      </view>
    </view>
  </view>

  <!-- 企业动态列表 -->
  <view class="news-list" wx:if="{{activeTab === 'news'}}">
    <view class="card news-card" wx:for="{{newsList}}" wx:key="id" bindtap="onNewsTap" data-id="{{item.id}}">
      <view class="flex flex-between mb-20">
        <view class="news-tag {{item.typeClass}}">{{item.type}}</view>
        <text class="news-date">{{item.date}}</text>
      </view>
      <view class="news-title mb-20">{{item.title}}</view>
      
      <!-- 大图展示 (如果有) -->
      <image class="news-cover-img mb-20" src="{{item.image}}" mode="aspectFill" wx:if="{{item.image}}"></image>
      
      <view class="news-desc mb-20">{{item.desc}}</view>
      
      <view class="news-footer flex">
        <view class="footer-item mr-20">
          <text class="icon">👁️浏览</text>
          <text>{{item.views}}</text>
        </view>
        <view class="footer-item mr-20">
          <text class="icon">❤️点赞</text>
          <text>{{item.likes}}</text>
        </view>
        <view class="footer-item mr-20">
          <text class="icon">💬评论</text>
          <text>{{item.comments}}</text>
        </view>
        <view class="footer-item">
          <text class="icon">转发</text>
          <text>{{item.comments}}</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 产品中心列表 (Grid) -->
  <view class="product-list-container" wx:if="{{activeTab === 'product'}}">
    <!-- 产品分类 -->
    <scroll-view scroll-x class="category-scroll mb-20" enable-flex>
      <view class="category-item active">全部</view>
      <view class="category-item">手机</view>
      <view class="category-item">穿戴</view>
      <view class="category-item">音频</view>
      <view class="category-item">家居</view>
    </scroll-view>

    <view class="product-grid">
      <view class="card product-card" wx:for="{{productList}}" wx:key="id" bindtap="onProductTap" data-id="{{item.id}}">
        <view class="product-img-box mb-20">
           <image class="product-img-large" src="{{item.image}}" mode="aspectFit" wx:if="{{item.image}}"></image>
           <view class="img-placeholder" wx:else>暂无图片</view>
        </view>
        <view class="product-info-center text-center">
          <text class="product-name-large">{{item.model}}</text>
          <text class="product-price">¥{{item.price}}</text>
        </view>
      </view>
    </view>
  </view>

</view>
